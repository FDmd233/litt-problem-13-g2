name: Render authorship QA

on:
  push:
    paths:
      - '.github/workflows/render-authorship-qa.yml'

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Render first page
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq poppler-utils
          mkdir -p /tmp/render
          pdfinfo output/pdf/Litt_Problem_13_G2.pdf | grep -F 'Author:          Zhijie He'
          pdftoppm -f 1 -singlefile -png -r 150 output/pdf/Litt_Problem_13_G2.pdf /tmp/render/page >/dev/null 2>&1
      - uses: actions/upload-artifact@v4
        with:
          name: g2-authorship-qa
          path: /tmp/render/page.png
